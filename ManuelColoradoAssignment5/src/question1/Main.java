package question1;

public class Main {
	
	public static void main(String[] args) {
		// Calls the menu class that handles the app in the constructor
		Menu menu = new Menu();
		// Calls the start menu
		menu.start();
	}
}
